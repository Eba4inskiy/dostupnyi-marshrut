# Optional browser integration. Never put server credentials in VITE_* values.
# Enable Maps JavaScript API + Places UI Kit API, billing, and HTTP referrer/API
# restrictions for this key. Its value will be visible in the browser bundle.
VITE_GOOGLE_MAPS_API_KEY=
